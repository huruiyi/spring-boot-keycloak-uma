# 待办事项

## 企业级权限模型改造

- [x] UI 权限清单：把菜单、页面、按钮等前端权限点纳入权限模型，统一映射到 `resource#scope`，避免前端散落硬编码。
  - [x] 在权限模型中增加 `uiPermissions` 结构，覆盖编码、名称、类型、所属页面、绑定权限、排序和启用状态。
  - [x] 同步初始化默认菜单和按钮权限点。
  - [x] 后台增加 UI 权限维护页面，并校验绑定的 `resource#scope` 必须存在。
  - [x] 前端从后端或模型接口加载 UI 权限清单，按编码探测和控制菜单/按钮。
- [x] 接口权限一致性校验：增强后端接口扫描，发现后端注解、系统接口权限、前端调用三者之间的漏配、错配和废弃项。
  - [x] 扫描 `@PreAuthorize` 里的权限表达式并生成接口权限候选。
  - [x] 校验系统接口权限是否引用不存在的 Permission。
  - [x] 校验后端已声明权限但未维护系统接口权限的接口。
  - [x] 校验前端引用了不存在或未维护的权限点。
- [x] 数据权限扩展点：在业务后端引入数据级授权检查，支撑本人、部门、金额阈值、租户等场景。
  - [x] 增加用户上下文解析，抽取用户、角色、组织、租户等属性。
  - [x] 增加数据权限策略接口，例如订单审批、订单查看范围。
  - [x] 在关键业务接口叠加 `resource#scope + 数据条件` 双重校验。
- [ ] 权限变更发布流程：支持草稿、差异预览、审批、发布、回滚和环境同步记录。
  - [ ] 增加权限模型版本号和变更说明。
  - [ ] 增加导入前差异校验和风险提示。
  - [ ] 增加发布历史和回滚能力。
  - [ ] 增加环境同步记录，区分本地、测试、生产。
