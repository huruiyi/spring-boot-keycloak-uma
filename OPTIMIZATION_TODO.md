# Optimization TODO

- [ ] Add frontend unit tests around `getRpt`, denied-cache behavior, and `apiFetch` authorization headers.
- [ ] Add request tracing and structured logs before introducing more API endpoints.
- [ ] Add a smoke-test script that starts dependencies and validates the happy path end to end.
